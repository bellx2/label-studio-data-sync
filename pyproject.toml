[project]
name = "car-data-v2"
version = "0.1.0"
description = "Fetch annotations from Label Studio and update the dataset"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "python-dotenv>=1.0",
    "requests>=2.32",
]

[tool.uv]
package = false
